module.exports = {
  config: {
    // ========== 基础配置 ==========

    // MD013: 行长度限制
    // 理由：数学公式、代码示例、表格等长行需要完整展示
    //      强制换行会影响可读性和复制粘贴
    MD013: false,

    // MD033: 链接语法（不允许 HTML 链接）
    // 理由：某些场景下使用 HTML 链接（如 `<a href="...">target="_blank"</a>`）更灵活
    //      例如需要在新标签页打开链接时
    MD033: false,

    // MD041: 第一行必须是标题
    // 理由：部分文件（如配置说明）可能不需要标题
    //      或以引用块、代码块开头
    MD041: false,

    // ========== 格式灵活性 ==========

    // MD024: 不允许重复标题
    // 理由：不同章节可能有相同的子标题（如"核心思想"）
    //      重复标题在同一章节内才需要避免
    MD024: false,

    // MD036: 强调文本不能作为标题
    // 理由：某些强调性文本（如"**记忆之前的输入，影响当前输出**"）
    //      虽然不是正式标题，但起到强调作用，增强可读性
    MD036: false,

    // ========== 代码和列表 ==========

    // MD029: 有序列表编号必须连续
    // 理由：嵌套列表或跨段列表时，重新编号（3, 4, 5）更清晰
    //      markdown 渲染器会自动调整为 1, 2, 3
    MD029: false,

    // MD040: 代码块必须有语言标识
    // 理由：ASCII 艺术图、流程图等非代码内容
    //      使用 ` ```plaintext ` 或 ` ```text ` 标识即可
    //      不强制指定编程语言
    MD040: false,

    // ========== 图片和媒体 ==========

    // MD045: 图片必须有 alt 文本
    // 理由：部分图表文件名已清晰描述内容（如 `attention.svg`）
    //      或 alt 文本为空字符串 `![ ]()` 时表示装饰性图片
    //      TODO: 后续逐步完善所有图片的 alt 文本
    MD045: false,

    // ========== 标题层级 ==========

    // MD001: 标题层级必须递增
    // 理由：部分归档文档（如 docs/archive/05-generative-models.md）
    //      存在 ## -> ### 跳跃的情况，历史遗留问题
    //      TODO: 后续重构时修复
    MD001: false
  },
  globs: ['docs/**/*.md', '*.md']
};
